import styled from 'styled-components'
import { desktopFontSize } from '../../styles'

const Title = styled.p`
  font-size: ${desktopFontSize.h2};
`

export const SimilarEvents = () => {
  return <Title>Similar Events</Title>
}
